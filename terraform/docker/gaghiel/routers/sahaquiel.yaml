http:
  routers:
    kuma:
      service: kuma
      rule: "Host(`kuma.home`)"
      entrypoints:
        - "web"
  services:
    kuma:
      loadBalancer:
        servers:
        - url: "http://192.168.1.210:3001"
